# 02476 MLOps (DTU)

Course workspace organized by sections.

## Structure
- `s1_.../`, `s2_.../`, ... each section is a separate uv project:
  - `pyproject.toml`
  - `uv.lock`
  - `.venv/` (not committed)

## Getting started (example)
```bash
cd s2_organization_and_version_control
uv sync
